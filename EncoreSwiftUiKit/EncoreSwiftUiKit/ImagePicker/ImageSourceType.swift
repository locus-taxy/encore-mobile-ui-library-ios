import Foundation

/// Type of image source for image picker.
/// Mirrors Android's `ImageSourceType` enum.
public enum ImageSourceType {
    case cameraOnly
    case galleryOnly
    case cameraOrGallery
}

